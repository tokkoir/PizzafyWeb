@page
@model LogoutModel
@{
    ViewData["Title"] = "Logout";
}

@{
    // This page will automatically redirect, so no content is needed
    // The OnGet method in the code-behind handles the logout logic
}